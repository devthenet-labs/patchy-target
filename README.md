# patchy-target

Deliberately vulnerable sample service for testing the patchy security pipeline.
It contains a path traversal (`/file`) and a reflected XSS (`/greet`) on purpose.

**Do not deploy this code.**
